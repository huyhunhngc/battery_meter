package io.github.ifa.glancewidget.ui.theme

import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme

val LightColorScheme = lightColorScheme(
    primary = app_light_primary,
    onPrimary = app_light_onPrimary,
    primaryContainer = app_light_primaryContainer,
    onPrimaryContainer = app_light_onPrimaryContainer,
    secondary = app_light_secondary,
    onSecondary = app_light_onSecondary,
    secondaryContainer = app_light_secondaryContainer,
    onSecondaryContainer = app_light_onSecondaryContainer,
    tertiary = app_light_tertiary,
    onTertiary = app_light_onTertiary,
    tertiaryContainer = app_light_tertiaryContainer,
    onTertiaryContainer = app_light_onTertiaryContainer,
    error = app_light_error,
    errorContainer = app_light_errorContainer,
    onError = app_light_onError,
    onErrorContainer = app_light_onErrorContainer,
    background = app_light_background,
    onBackground = app_light_onBackground,
    surface = app_light_surface,
    onSurface = app_light_onSurface,
    surfaceVariant = app_light_surfaceVariant,
    onSurfaceVariant = app_light_onSurfaceVariant,
    outline = app_light_outline,
    inverseOnSurface = app_light_inverseOnSurface,
    inverseSurface = app_light_inverseSurface,
    inversePrimary = app_light_inversePrimary,
    surfaceTint = app_light_surfaceTint
)

val LightColors = lightColors(
    check = routine_secondary50,
    amber = amberLight,
    blue = blueLight,
    brown = brownLight,
    gray = grayLight,
    green = greenLight,
    indigo = indigoLight,
    lime = limeLight,
    orange = orangeLight,
    red = redLight,
    pink = pinkLight,
    teal = tealLight,
    yellow = yellowLight
)

val DarkColorScheme = darkColorScheme(
    primary = app_dark_primary,
    onPrimary = app_dark_onPrimary,
    primaryContainer = app_dark_primaryContainer,
    onPrimaryContainer = app_dark_onPrimaryContainer,
    secondary = app_dark_secondary,
    onSecondary = app_dark_onSecondary,
    secondaryContainer = app_dark_secondaryContainer,
    onSecondaryContainer = app_dark_onSecondaryContainer,
    tertiary = app_dark_tertiary,
    onTertiary = app_dark_onTertiary,
    tertiaryContainer = app_dark_tertiaryContainer,
    onTertiaryContainer = app_dark_onTertiaryContainer,
    error = app_dark_error,
    errorContainer = app_dark_errorContainer,
    onError = app_dark_onError,
    onErrorContainer = app_dark_onErrorContainer,
    background = app_dark_background,
    onBackground = app_dark_onBackground,
    surface = app_dark_surface,
    onSurface = app_dark_onSurface,
    surfaceVariant = app_dark_surfaceVariant,
    onSurfaceVariant = app_dark_onSurfaceVariant,
    outline = app_dark_outline,
    inverseOnSurface = app_dark_inverseOnSurface,
    inverseSurface = app_dark_inverseSurface,
    inversePrimary = app_dark_inversePrimary,
    surfaceTint = app_dark_surfaceTint
)

val DarkColors = darkColors(
    check = routine_secondary80,
    amber = amberDark,
    blue = blueDark,
    brown = brownDark,
    gray = grayDark,
    green = greenDark,
    indigo = indigoDark,
    lime = limeDark,
    orange = orangeDark,
    red = redDark,
    pink = pinkDark,
    teal = tealDark,
    yellow = yellowDark
)
